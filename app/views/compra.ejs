<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/public/css/style-compra.css">
</head>
<body>
    <main>
        <header class="header">
            <div class="logo"><a href="/">LOGO</a></div>
            <input type="search">
            <nav>
                <ul class="nav-links">
                    <li><a href="/">Home</a></li>
                    <li>
                        <%if (nombres) { %>
                            <a href="#"><%= nombres %></a>
                        <% } %>
                    </li>
                    <li>
                        <%if (!nombres) { %> 
                            <a href="/registro-interfaz">Register</a>
                        <% } %>
                    </li>
                    <li>
                        <%if (!nombres) { %> 
                            <a href="/login-interfaz">Login</a>
                            <% } %>
                    </li>
                    <li>
                        <% if(nombres) { %>
                            <a href="/logout">Exit</a> 
                        <% } %> 
                    </li>
                </ul>
            </nav>
        </header>
        <div class="info-compra">
            <p>Gracias señor@ <%= nombres %> por comprar en nuestra tienda. Su artículo está en
                camino. <a href="/tienda">Seguir comprando</a></p>                
        </div>
    </main>
</body>
</html>